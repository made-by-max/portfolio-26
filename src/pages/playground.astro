---
import { getCollection } from "astro:content";
import PlaygroundCardSection from "../components/PlaygroundSection.astro";
import BaseLayout from "../layouts/BaseLayout.astro";

const posts = await getCollection("playground");
// posts.sort((a, b) => {
//     const dateA = new Date(a.data.date);
//     const dateB = new Date(b.data.date);
//     return dateB.getTime() - dateA.getTime();
// });

const heading = "Playground";
const pageTitle = "Playground";
---

<BaseLayout pageTitle={pageTitle}>
    <div class="wrapper-wide sidebarLayout">
        <h1>{heading}</h1>
        <p class="subhead">
            Prototypes, experiments, and smaller coding projects
        </p>
        <!-- <div class="main-content"> -->
        <article>
            <PlaygroundCardSection />
        </article>
    </div>
</BaseLayout>

<style>
    h1 {
        margin-block-end: var(--size-2);
    }
    .subhead {
        font-size: var(--text-2xl);
        line-height: var(--text-2xl-line-height);
        font-weight: var(--font-weight-normal);
        color: var(--text-soft);
        margin-block-end: var(--size-10);
    }
</style>
