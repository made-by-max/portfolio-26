---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import CaseStudySection from "../components/CaseStudySection.astro";
import ProductDesignBlogSection from "../components/ProductDesignBlogSection.astro";
import "../styles/cards.css";
const posts = await getCollection("design");
import SectionHeader from "../components/SectionHeader.astro";

const heading = "Selected Work";
const pageTitle = "Selected Work";
---

<BaseLayout pageTitle={pageTitle}>
    <section>
        <div class="wrapper-wide">
            <h1 class="">Selected Work</h1>
            <p class="eyebrow">Product design case studies</p>
            <CaseStudySection />
        </div>
    </section>
    <section>
        <div class="wrapper-wide">
            <SectionHeader
                headerText="Related Writing"
                linkText="All posts"
                link="/writing"
            />
            <ProductDesignBlogSection />
        </div>
    </section>
</BaseLayout>

<style>
h1 {
    margin-block-end: var(--size-2);
}
.eyebrow {

    margin-block-end: var(--size-10);
}

    section {
        margin-block: 180px;
    }
</style>
