---
import type { CollectionEntry } from "astro:content";
import "../styles/blogpost.css";

const { post } = Astro.props;
---

<div>
    <!-- <svg
        xmlns="http://www.w3.org/2000/svg"
        width="24"
        height="24"
        viewBox="0 0 24 24"
        ><path
            fill="none"
            stroke="currentColor"
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M12 2H2v10l8.5 8.5q1.5 1.4 3 0l7-7q1.4-1.5 0-3ZM7 7"></path></svg
    > -->
    {
        post.data.tags.map((tag) => (
            <p class="post-tags">
                <a class="tag" href={`/tags/${tag}`}>
                    {tag}
                </a>
            </p>
        ))
    }
</div>

<style>
    div {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--size-1);
    }
</style>
