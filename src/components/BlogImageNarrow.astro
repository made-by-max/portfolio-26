---
import { Image } from "astro:assets";

const { src } = Astro.props;
const { alt } = Astro.props;
---

<Image src={src} alt={alt} />

<style>
    img {
        width: var(--max-text-length);
        margin-block: var(--size-12);
    }
</style>
