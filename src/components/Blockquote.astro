---
---

<blockquote>
  <slot />
</blockquote>

<style>
  blockquote {
    background-color: color-mix(in srgb, var(--brand-primary, #ff5c00) 10%, transparent);
    border-left: 8px solid var(--brand-primary, #ff5c00);
    padding: 40px 48px;
    margin-block: var(--size-8, 2rem);
    margin-inline: 0;
    max-width: var(--max-text-length);
  }

  blockquote :global(p) {
    font-style: italic;
    font-size: var(--text-xl);
    line-height: 1.54;
    color: var(--text #111);
    margin: 0;
    max-inline-size: none;
  }
</style>
