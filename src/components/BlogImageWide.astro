---
import { Image } from "astro:assets";
const { src, alt } = Astro.props;
---

<!-- <div class="brutalist-img-wrap img-wrap-wide">
  <!-- Orange shadow card at BOTTOM-RIGHT -->
  <!-- <div class="brutalist-shadow" aria-hidden="true"></div> -->

    <Image src={src} alt={alt} class="card-img" />



<style>

img {
  border-radius: 11px;
  border: 2px solid var(--card-stroke, #111);
  box-shadow: 4px 4px 0px 0px var(--card-stroke);
  margin-block: var(--size-12-fluid);
  background-color: var(--background-primary, #f3f4f2);
}

/*.case-study-img {
     box-shadow: 4px 4px 0px  0px var(--card-stroke);

      border-radius: 11px;
      margin-block: var(--size-12-fluid);
      background-color: var(--background-primary, #f3f4f2);
}*/




  /*.img-wrap-wide {
    max-width: var(--wrapper-medium);
  }*/

  /* Wide image uses a slightly flatter rotation */
  /*.brutalist-shadow {
    transform: rotate(0.73deg);
  }*/

  /*.inner-card {
      box-shadow: 6px 6px 0px  0px var(--brand-primary);
       transition: box-shadow 150ms ease, transform 150ms ease;

        border-radius: 11px;
        position: relative;
        background-color: var(--card-background, #f3f4f2);
        border: 2px solid var(--card-stroke, #111);
        border-radius: 11px;
        overflow: hidden;
        z-index: 1;
        /*height: 100%;*/

  /*}


  .inner-card:hover,
  .inner-card:focus {
    box-shadow: 3px 3px 0px 0px var(--brand-primary);
    transform: translate(2px, 2px);
    text-decoration: none;
  }*/
</style>
